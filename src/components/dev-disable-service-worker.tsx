"use client";

// Questo componente è stato svuotato intenzionalmente.
// L'app non usa Service Worker, quindi non c'è nulla da pulire.
// Il vecchio codice di cleanup causava un reload automatico su Chrome/Edge/Safari.
export function DevDisableServiceWorker() {
  return null;
}